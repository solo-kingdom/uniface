graph_id: echo
version: v1
entity_type: lab.Generic
schema_version: v1
entry: hello
nodes:
  hello:
    kind: compute
    unit: lab.hello
    transitions:
      - target: echo
  echo:
    kind: compute
    unit: lab.echo
    transitions:
      - target: done
  done:
    kind: terminal
    outcome: success
